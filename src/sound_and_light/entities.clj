;; Entities methods
(ns sound-and-light.entities)

;; TODO
(defn get-with-components
  "gets all the entities with the components"
  [entity-map components]
  {})

(defn change-entities
  "replaces the changed entities"
  [entity-map new-entities]
  {})